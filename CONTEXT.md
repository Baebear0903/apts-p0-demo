# APTS Domain

本词汇表只解释业务术语。本次 Demo 的功能、范围和行为以 SPEC 为准；这里的产品通用或 P1／P2 定义不产生额外实现任务。

## Language

**APTS**:
全院通用的患者识别与运营编排平台。它拥有识别、复核、人群治理和数据开放配置，并可使用经数据底座接入的外部反馈数据；它不执行患者触达、管理接口交付状态、办理医疗资源或承担必须逐条闭环的实时临床告警。
_Avoid_: 患者 CRM、一人一档、端到端患者服务系统

**统一门户**:
APTS 外部的医院统一身份与权限管理入口，管理用户、角色、科室以及 APTS 菜单和按钮的功能权限。
_Avoid_: APTS 用户中心、APTS 角色配置

**数据底座**:
APTS 患者数据判断的唯一数据来源，通过已接入的 CDR、患者时序数据及指标提供计算输入和证据。“无记录”等判断均限定在数据底座提供的数据范围内。
_Avoid_: APTS 自行采集外部数据、底座外患者事实

**数据机构**:
数据底座中用于标识就诊记录所属医院、院区或科室的组织字典；其编码通常与统一门户组织相同，但两边可能不同步，APTS 不据此自动将二者等同。
_Avoid_: 门户组织、责任组织

**APTS 数据集**:
由后台配置后导入 APTS 的 CDR／患者时序数据库预置数据对象及字段集合，明确其患者、就诊、记录与业务时间等关联语义。前台入口称为「数据集」，只供查看和选择，不提供新增、修改、删除或停用，也不从该页发起指标配置；指标绑定和衍生计算在指标库完成。它不是 APTS 自建的专病数据集、对外数据集交付或已圈选患者人群。
_Avoid_: 数据查看、数据集配置、人群快照、数据集交付、专病患者库

**指标库**:
按业务目录组织的 APTS 可用变量／指标及其数据绑定、取值和规则使用属性的集合。标签逻辑通过这些配置使用底座数据；其中可以包含直接绑定字段的变量，不要求每项都是统计计算结果。
_Avoid_: 标签库、患者数据副本

**功能权限**:
由统一门户按角色授予的 APTS 菜单可见性和按钮操作权，不等同于对具体标签等业务对象的使用授权。
_Avoid_: 标签授权、患者数据范围

**标签使用授权**:
APTS 后台按统一门户角色配置的标签白名单。授权某复合标签不等于授权其引用的基础标签，白名单不沿引用关系扩大。某角色能否对某标签执行一项动作，由门户功能权限与该授权共同决定，不再为单个标签拆分动作权限。
_Avoid_: 标签数据授权、逐标签按钮权限、引用闭包授权

**患者范围**:
APTS 从数据机构中手工选择的全院或多院区患者范围，可绑定门户角色并用于数据开放配置。患者只要存在某院区就诊记录即属于该院区，并可同时属于多个院区；全院范围是各院区就诊归属的并集，不是数据底座中的全部对象。P1 仅全院范围可打开「包含无院区就诊的建档对象」；单一院区或多院区范围不能打开。进入范围后可使用其全部已接入数据，不按数据来源裁剪证据。
_Avoid_: 业务范围、门户组织自动映射、数据来源院区、底座全量对象

**无院区就诊的建档对象**:
数据底座中已有主患者标识、但没有任何院区就诊记录的人。默认不属于院区范围或全院范围。P1 仅当全院范围显式打开纳入开关后才进入该全院范围；不另建第三种范围类型，也不能算进某一院区。
_Avoid_: 自动纳入全院、因数据在底座而默认可见、院区范围夹带全市居民

**权限判定**:
涉及标签实时使用的动作须由同一角色具备功能权限、标签使用授权和患者范围。固定快照的查看与交付按功能权限和患者范围校验，来源标签证据另校验白名单，来源标签已删除不阻断快照消费。用户最终可执行的是各角色完整授权的并集，不能把不同角色的动作、标签和范围分别取并后交叉使用。
_Avoid_: 分维合并后交叉、权限乘积

**职责模板**:
Spec 中用于说明标签维护、运营处理、数据开放配置、APTS 业务管理和管理查看等责任的参考分工；医院可将其组合为统一门户角色，APTS 不把岗位名称写死为系统角色。
_Avoid_: APTS 内置角色、医院岗位

**责任组织**:
对标签、人群、识别结果或数据开放配置承担业务责任的门户组织，与经办人和该对象的患者范围分开记录。
_Avoid_: 个人所有者、默认可见范围

**运营编排**:
把已识别的患者或人群转化为可确认、可治理并可向外部授权开放的业务能力。
_Avoid_: 直接触达、服务执行、接口运行管理

**工作台**:
按当前用户权限展示模块当前规模、待办摘要、数据开放摘要和各模块入口的首页。数字只表示当前规模，不含趋势、人次或效果。识别待办按当前工作上下文中的患者范围尚未确认的最新批次计。事项必须回到所属模块处理，工作台不复制完整业务列表、不直接复核或启停开放、不导出，也不替代 P2 运营分析。
_Avoid_: 待办处理中心、运营分析看板、效果分析首页

**运营分析**:
APTS 的 P2 内部标签使用与管理分析能力，统计自动识别规模、人群形成、数据开放授权及复核使用反馈，不以人群快照或触达数据为前提。患者运营效果分析留到后续项目按场景定制，不属于本次标准产品的分析能力。
_Avoid_: 第三方执行监控、患者触达效果分析、通用分析主题配置

**命中人数**:
在明确的统计期间、标签和患者范围内，按主患者标识去重后的命中患者数；跨标签汇总整体人数时再次去重，不直接相加各标签人数。
_Avoid_: 命中人次、各标签人数之和

**命中人次**:
按标签和计算批次累计的患者命中次数；同一患者在同一标签的同一批次内计一次，跨批次重复命中分别计数。统计口径不改变各批次保存的命中结果。
_Avoid_: 去重患者人数、就诊人次

**处置建议**:
标签的可选文字建议，可在标签详情维护及单标签相关患者详情查看，供患者服务参考；不是临床医嘱、强制任务或具体执行流程，多标签订阅不自动下发该建议。
_Avoid_: 服务意图、治疗方案、触达任务

**标签订阅**:
APTS 授权已对接系统按需查询或定时全量推送、持续获取满足指定条件的患者及最小必要字段。条件来自一条动态人群：纳入标签须全部命中，排除标签命中任一即去掉，不另配且／或。计算使用该动态人群的当前条件、其中标签的当前逻辑和当前可用患者数据；结果是患者清单，不是患者×标签明细。不依赖标签是否开启自动识别。
_Avoid_: 人群订阅（第三种开放类型）、标签原料通道、任意患者查询、服务任务

**订阅结果版本**:
一次标签订阅计算成功后形成的可交付患者清单，以结果标识和计算完成时间区分各次结果；它不表示标签规则有历史版本。
_Avoid_: 自动识别批次、人群快照、实时患者状态

**已对接系统**:
已经完成线下联调、可被数据开放配置选择的外部系统登记项，声明其可用交付方式并引用数据底座所提供的数据开放通道。
_Avoid_: 目标系统类型、在线接口开发、第三方系统字典

**数据开放配置**:
将已确认的人群快照或动态人群按指定患者范围、规定的字段集合、方式、期限和用途授权开放的业务配置对象；有相应按钮权限的人员可直接维护、启用、暂停及恢复，不设审批；类型为数据集交付或标签订阅，P0各类型字段集合固定，不提供自由字段配置。界面状态「授权已启用」只表示授权生效，不表示对方已查询、接收或使用数据。
_Avoid_: API、服务交接单、开放接口、交付成功

**数据集交付**:
把已确认的人群快照作为固定结果，通过受控直接导出或已对接通道一次性交付。直接导出仅在授权启用后提供，草稿、暂停和到期不能下载。交付后不随标签或患者数据变化，已交付数据不承诺从使用方或外部系统撤回。
_Avoid_: 临时查询导出、动态人群、标签订阅

**回流数据**:
第三方患者服务产生并经数据底座接入 CDR 或时序数据库的患者反馈数据，进入 APTS 后与其他患者数据一样可供指标、标签计算和相关证据使用。
_Avoid_: 接口回执、交付状态、APTS 外部任务闭环

**主患者标识**:
医院患者主索引为同一患者分配的跨系统统一身份标识，是 APTS 与外部系统关联患者的默认依据。
_Avoid_: APTS 患者编号、手机号

**外部执行系统**:
完成短信、电话、随访、预约、加号或住院办理等实际业务动作的第三方系统。
_Avoid_: APTS 内置服务

**标签定义**:
用于判断患者是否命中某项业务语义的当前计算逻辑，可以由单一指标条件或多条件逻辑树组成。已发布标签可由有编辑权限者保存当前逻辑修改；标签不保存规则历史版本，也不提供版本回滚。
_Avoid_: 指标、标签人群、患者标签结果

**试算**:
配置标签时用当前规则对当前可用数据做的非正式查询，只展示命中人数和脱敏样例，不形成计算批次、命中清单或人群。
_Avoid_: 计算批次、命中清单、识别结果

**计算批次**:
一次需要保留业务结果的标签计算，包括用户确认生成人群快照的主动检索、已保存动态人群的正式刷新，以及标签按配置周期自动识别形成命中清单。试算和用户调整检索条件时的中间试查都不是计算批次。
_Avoid_: 打标结果、标签人群

**数据刷新时间**:
当前展示的患者清单计算完成的时间，用于说明这批患者是什么时候计算出来的；它不等同于标签规则修改时间，也不代表所有源数据已更新到这一时刻。
_Avoid_: 标签规则更新时间、源数据截止时间

**人群**:
已经保存、可继续查看和复用的患者集合。用户可按标签纳入/排除条件主动创建动态人群或快照人群；自动识别结果经复核后只生成快照人群。临时检索结果和未经复核的识别结果都不是人群。
_Avoid_: 临时查询结果、待确认识别批次、复核清单

**命中清单**:
某个计算批次产生的患者清单，只包含满足标签定义的患者，不保存未命中患者。每位患者的命中明细是关联证据的最小记录。
_Avoid_: 患者标签结果、全量患者清单

**主动人群**:
用户保存的标签圈选条件及其患者集合。动态人群以最近一次成功计算作为当前成员并保留历次批次，快照人群固定保存用户确认后保留的成员，并关联来源计算批次与原始命中清单。
_Avoid_: 临时查询、命中清单

**动态人群**:
人群管理中收藏的标签纳入／排除条件，不是一份冻住的名单。纳入须同时命中全部纳入标签，排除命中任一排除标签即去掉，不配置且／或。保存时可保留与当前条件对应的完整计算结果作为当下成员，人工移除只作用于待确认快照，不写入动态人群；无对应成功结果时仅保存条件。人群页可以按条件算出当前成员并手动刷新，仅供查看和确认快照；标签订阅绑定该对象的当前条件现算患者，不使用某次刷新看到的名单。当前用户缺少条件中任一标签的使用授权时，不能刷新或用它做订阅计算，并提示缺授权；对象对仍有授权的其他用户保持可用，不因此全局暂停。
_Avoid_: 开放用现成名单、第三种开放类型、且或组合器、缺授权即全局停用

**自动识别配置**:
单个已发布标签上用于周期性识别人群的业务配置，P0 只包含是否启用和扫描间隔；多条件识别由复合标签表达，不另设策略对象或业务类型配置。
_Avoid_: 自动识别策略、预警标签、被动标签

**识别中心**:
集中查看已开启自动识别的标签、最新及历史识别批次，并对当前工作上下文中的患者范围尚未确认的最新批次执行复核的业务入口。每次确认只作用于当前患者范围。P0 默认全院，无范围选择步骤；P1 多范围场景才需选择工作上下文。某一患者范围确认后，该范围对该批次只读；其他患者范围不受影响。范围内无命中患者时不出现待确认操作。新周期产生新批次后，上一批次对所有范围转为只读历史。自动识别开关和扫描间隔仍在标签中心配置。
_Avoid_: 自动识别（菜单标题）、识别任务、预警中心、多范围一次合并确认

**人群快照**:
用户检查主动查询结果或自动识别结果后，确认保存的固定患者集合。自动识别确认按患者范围各生成一张快照，只含该范围与批次命中的交集中确认保留的成员；主动查询的确认并保存同样只保存当前工作上下文中的患者范围所见结果。授权变化不删除或改写已保存成员；当前用户只能看到仍在其患者范围内的人，来源标签不在白名单时只保留身份与就诊摘要、隐藏该标签证据。两种来源生成同一种业务对象，统一进入人群管理和后续消费，只保留来源用于追溯；人群快照不等同于未经确认的命中清单。快照成员不原地修改，需调整时从原快照移除成员并生成新快照。历史快照永久保留，来源标签删除后仍可查看、直接导出及用于新数据集交付，继续受功能权限与患者范围约束，不依赖标签实时计算。
_Avoid_: 待确认标签、标签复核、跨范围共用一张确认快照、撤权即删快照

**患者画像**:
从识别结果或人群成员进入的上下文详情，保留进入来源，展示可识别患者的身份信息、就诊科室等关键就诊信息，以及相关标签、触发时证据、标签相关证据时间轴和当前复核状态。相关标签只列出当前用户标签使用授权白名单内的标签；复合标签的证据树可以展示被引用基础逻辑及其贡献，不把那些基础标签列入相关标签，也不能跳转到其独立命中清单。它不是独立患者工作区，也不是全量病历档案。
_Avoid_: 一人一档、患者档案、全量病历、证据展示即授权

**待确认识别批次**:
标签每次自动识别产生的不可改写命中结果。最新批次按患者范围分别处于待确认或已确认：某范围确认后，该范围即使面对时间上的最新批次也立即只读。新批次产生后，上一批次对所有范围转为只读历史。同一患者可以在多个业务周期重复出现，也可以出现在多个患者范围各自确认的快照中。
_Avoid_: 持续任务、动态待办清单、临床告警、整批单一确认状态

**复核清单**:
最新待确认识别批次在某一患者范围下的业务视图，成员为该范围与命中清单的交集，不是另一份计算结果。人工可移除其中患者但不可手工添加，确认后剩余成员大于0才形成人群快照，全部移除只留下零人确认记录；处理不改写原始命中清单。
_Avoid_: 标签候选、待确认标签、主动人群确认

**复核**:
工作人员检查待确认的最新识别批次、移除不应进入后续业务的患者并确认形成人群快照的业务动作。确认作用于当前患者范围与该批次命中清单的交集；「已处理」只对绑定了同一患者范围的用户显示，不跨范围代替或阻止其他范围的确认。「当前已不满足」只作提示，不自动移除，也不阻止确认。主动查询入口中的人工判断称为“确认并保存人群”，不使用「当前已不满足」。
_Avoid_: 标签复核、修改原始命中、跨范围传染已处理

**基础标签**:
计算逻辑直接由指标条件构成的标签定义。完成配置并发布后，可被复合标签作为前置逻辑复用；引用已发布基础标签不要求维护者拥有该标签的使用授权。
_Avoid_: 原始指标、上游标签、引用须先有使用授权

**复合标签**:
在逻辑树中复用已发布基础标签的当前生效逻辑，并可混合新增指标条件、条件分组及“且／或”关系的标签定义。它无需重复配置基础标签逻辑，不依赖基础标签预先产生命中清单；标签引用仍限于基础标签。维护复合标签需要对它有标签使用授权及编辑／发布功能权限，并可引用任何已发布基础标签。计算时展开被引用基础标签的当前逻辑，证据按该复合标签的逻辑树解释命中原因；这不把那些基础标签加入使用者的白名单，也不允许将其用于圈选、订阅或跳转到独立命中清单。
_Avoid_: 下游标签、复制基础标签逻辑、引用即授权

**标签不可用**:
标签因自身停用／删除、引用的基础标签停用／删除或依赖的指标停用，不能继续用于新业务计算的状态，使用时须明确提示原因。停用依赖恢复后，因该依赖中断的标签使用自动恢复，独立人工暂停保持有效；已删除依赖不能恢复，须显式修复规则。已有清单和证据仍按原业务结果保留。
_Avoid_: 零命中、患者未命中状态

**衍生变量／指标**:
数据底座已有可用指标之外，根据已接入变量或指标的明确计算关系配置出的可复用计算结果。在对接底座、配置可用变量或指标时维护，供标签逻辑直接引用；底座已有的同类结果优先直接使用。
_Avoid_: 标签条件内重复维护指标公式、基础标签

**判断方式**:
标签条件对所选数据作出判断的方式，例如直接比较、最新一次、存在、累计次数、最近 N 次每次均满足、聚合或变化比较；适用方式由数据对象的能力决定。
_Avoid_: 次数维度、页面模式

**有效记录**:
在所选规则与指标声明下能够参与判断的记录，包含可判断的正常和异常记录。按有效观察计次时，无法判断的采集不计入次数；正常记录参与排序，不能为凑足异常次数而排除。
_Avoid_: 命中记录、仅异常记录

**记录条件组**:
在同一患者的一次记录、采集或就诊内组合并评价条件，再对这些观察进行存在性、次数或最近 N 次等判断的规则组成部分。可复用支持逐次评价的基础标签逻辑，须保留其原有含义及关联约束。
_Avoid_: 重复复制条件、按扫描批次计临床次数

**同次就诊限定**:
标签逻辑树中要求指定条件在同一次就诊内成立的额外约束。所有条件始终围绕同一患者计算，不添加该限定时不要求同次就诊。
_Avoid_: 默认同次就诊、跨患者条件组合

**标签相关证据时间轴**:
在患者画像中按业务时间展示与所展示标签有关的 CDR 和时序数据库关键数据快照，用于解释本次计算的标签命中依据。所展示标签为复合标签时，时间轴可包含被引用基础逻辑对应的证据，仍只解释该复合标签为何命中。
_Avoid_: 全量病历时间轴、全量患者档案、把引用标签当作独立授权对象展示

**触发时证据**:
来源计算产生标签命中时保存的关键数据快照，用于解释患者为何进入本次结果；之后确认形成人群快照不改变该证据及其对应时间。
_Avoid_: 当前患者状态

**当前复核状态**:
使用当前最新数据和当前最新生效标签定义重新校验得到的判断。只有明确不再命中时才提示“当前已不满足”；该提示不覆盖触发时证据，也不自动改变待确认识别批次。
_Avoid_: 第二套实时证据链、实时改写历史命中

**复核记录**:
工作人员在某一患者范围下确认或移除复核清单成员时形成的业务记录，包含该患者范围、复核时间、复核人、处理结果、移除原因、当时的当前复核状态摘要，以及因此生成的人群快照。同一批次的不同患者范围各自留下记录，互不改写。
_Avoid_: 改写原始规则命中、手工标签、整批共用一条确认记录


**已删除标签**:
标签删除后的终态。已发布标签须先停用，删除停止新增计算与引用，并使依赖它的规则、动态人群、订阅及当前计算失效；不能略过该条件继续计算。历史记录保留标签原名称、稳定标识和已删除标记，稳定标识不复用。
_Avoid_: 清空历史、自动移除依赖条件、零命中

**历史业务记录**:
已经形成的计算批次、命中及触发证据、复核记录、人群快照、固定交付及订阅结果。它们永久保留，不因来源标签删除或后续清理规划被改写或移除；历史存在不代表当前仍可计算，也不扩大当前访问权。
_Avoid_: 当前实时结果、标签历史版本、删除标签即删记录

**开放有效期**:
数据开放配置允许提供数据的时间范围。启用立即生效，可长期有效或设定截止时间；到期停止开放，调整期限后须由有权限人员恢复。它与按需查询最小间隔和定时推送周期分别表达。
_Avoid_: 查询周期、推送周期、预约生效、审批期限

**事件锚点**:
用于确定事件前后时间窗口的已发生业务事件，可选择首次、最近一次或任一次，默认最近一次；没有锚点则对应条件不命中。任一次按各事件分别评价，不能拼接不同事件窗口内的局部证据。
_Avoid_: 未来事件、默认只有一次事件

**较前变化**:
默认对最近两次可比有效记录求绝对变化或变化率；绝对变化为本次减前次，变化率为差值除以前次绝对值。前次为零时变化率无法判断，不足两次可比记录不命中；单位转换与可比性由指标声明。
_Avoid_: 零基准变化率为零、猜测单位换算


**使用方**:
获得开放数据并用于所填写业务用途的组织或业务团队，不是具体经办人，也不等同于已对接系统。直接导出记录使用方；通道交付和订阅还须绑定已对接系统。
_Avoid_: 接收方、经办人、接收系统

**数据开放通道**:
数据底座在APTS之外已经对接的交付或查询能力，APTS通过登记标识选择它；底座同时承担数据查询与开放技术服务，文中的数据/API底座是同一职责边界的称呼，不要求是同一物理系统。
_Avoid_: APTS接口开发、前台接口地址配置

**计算时规则说明**:
随业务计算结果固定保存、用于解释当时为何进入结果的条件内容：自动识别记录来源标签逻辑，人群计算记录纳入／排除关系及各标签展开逻辑，均包含当时参数与相关指标取值定义。它不随当前规则变化，也不是可切换或回滚的标签历史版本。
_Avoid_: 标签版本管理、用当前规则解释历史命中


**连续日期满足**:
指定时间窗口内存在N个相邻自然日，每天均按所选口径满足观察条件；每日口径为“当天存在一次满足”或“当天全部有效观察满足”，默认前者。每天至少有一条可判断观察，没有可判断观察的日期打断连续；它不同于允许跳过无法判断观察的最近N次判断。
_Avoid_: 最近N次有效观察、持续异常时长、跳过缺日


**来源条件**:
形成当前所查看业务结果时实际使用的筛选逻辑，可以是自动识别的单个标签逻辑，也可以是主动圈选、动态人群或订阅使用的纳入／排除组合条件；历史查看采用计算时固定保存的内容。
_Avoid_: 所有结果都有唯一来源标签、当前条件覆盖历史条件


**默认使用方**:
已对接系统登记的表单预填值，仅在未启用开放配置的使用方为空时带入，不限制该系统可以服务的业务团队。开放配置记录独立的实际使用方，启用前可修改、启用后锁定，系统默认值变更不回写已有配置。
_Avoid_: 系统授权使用方、使用方白名单、强制一致性约束
