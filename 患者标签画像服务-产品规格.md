# 患者标签画像服务（APTS）· 产品规格

状态：P0 需求评审 Demo 实施输入（2026-09-08）  
读者：产品、研发、实施与 Demo 实现者  
本文完整描述业务行为、页面、字段、样例与验收。配套领域词汇表只解释术语。无需其他文件、原型或外部资料才能理解和实现本规格。

本文定义 P0 Demo 的业务要求。实现者自行选择前端技术栈、组件及未限定的视觉细节；不得自行增加业务功能或缩减规则能力。P1／P2 是背景，只有明确标为占位或只读的现有入口需要呈现。

---

## 0. 用本文生成 Demo

生成目标：一份可点击的 **P0 全功能需求评审原型**（虚拟数据、不连真实医院库）。故事线只用于串讲，不能用来裁功能。

### 阅读与执行范围

- SPEC 决定本次功能、页面、行为和验收；CONTEXT 只统一术语，不增加页面、后台或后续分期功能。无需查找其他资料。
- 产品中的门户、数据库、定时服务、永久历史是业务边界。Demo 只用本地虚拟数据、共享会话状态和可推进的演示时钟；不建设这些生产服务。会话内删除标签不能删除历史，刷新／重置可恢复初始样例，不要求跨会话永久存储。
- P0 标为“完整”的功能须可操作；“占位”只显示标题和说明，“只读”只展示预置内容。运营分析虽标 P2，本次仍保留规定的占位入口。
- 开发顺序可自行安排，但最终覆盖全部操作及规则验收。第3章、13章、14.1节仅说明边界，不据此新增专科模块、多院区、模型管理或审计功能。

### 0.1 必须同时满足

1. 第 14 章矩阵中 **Demo 覆盖 = 完整** 的每一行都有对应页面、操作和状态。
2. 第 9 章的空态／必要异常都能走到（0 人、当前已不满足、依赖不可用、标签删除后依赖失效、计算失败 Toast、草稿不能导出）。
3. 导航、布局、操作及状态均按本文完整实现。
4. 界面文案使用本文明确的业务术语，不用「预警任务」「服务策略」「交付成功」「一人一档」。
5. Demo 使用第 9 章预置数据包；配置类操作写进会话状态，刷新可重置。

### 0.2 Demo 技术边界

| 做 | 不做 |
|---|---|
| 单页或多页前端，虚拟 JSON／内存状态 | 真实 CDR／时序库、统一门户登录、生产鉴权 |
| 配置保存后，后续计算／列表按新配置展示 | 真实全院计算与生产规则引擎；仅在有限虚拟数据上按当前配置计算，不按患者名、标签名或固定布尔标记硬编码命中 |
| 依赖停用／删除、按钮权限、开放启停与到期在界面生效 | 接口调用日志、推送失败重试、第三方是否已收到数据 |
| 评审演示账号具备走通主链路所需的功能权限 | 多用户登录；P0 不演示多院区切换 |

评审演示账号（Demo 约定，不是产品内置角色）：全院患者范围；样例标签及演示中新建标签均在演示账号白名单；可维护标签、复核、圈选、配置及启停开放、查看系统管理。此新建自动授权仅为 Demo 便利约定，不推导为生产授权政策。标签使用授权矩阵页 **占位**；患者范围页只读展示「全院」。

演示辅助工具集中在可折叠的“演示控制”区：重置数据、推进时间、模拟一次失败、切换单项按钮权限、载入规则样例。它们不属于业务菜单，不做用户／角色管理页。默认关闭，正常页面只显示业务内容。按需查询没有历史成功结果时首次允许计算；已有结果按最小间隔重算。定时推送首次时点为启用时点加所选周期；模拟下一周期使用同一演示时钟，保持业务时间与数据一致。

### 0.3 串讲路径（覆盖检查仍以矩阵为准）

1. 系统管理：看数据集 → 指标库（眼表评分）→ 标签中心编辑「眼科复诊运营筛选」→ 试算。
2. 工作台「识别结果已更新」→ 识别中心最新批次 → 点李强看「当前已不满足」→ 移除 → 确认快照。
3. 人群管理：新建「健康活动圈选」（纳入成年患者，排除眼科复诊运营筛选）→ 保存动态人群 → 开放订阅。
4. 数据开放：基于刚确认的快照新建并启用「眼科复诊名单交付」→ 状态为「授权已启用」→ 直接导出。旁注写明不表示对方已收到数据。

非正常分支至少再演示：指标或基础标签停用导致不可用；数据开放暂停／到期；检索 0 人不能存快照。

### 0.4 视觉与可用性底线

- 桌面 Web；左侧一级导航 + 主区列表／详情；不为分类再加常驻二级菜单（系统管理五个子页除外）。
- 核心流程可键盘操作，焦点可见，表单标签与错误可读，状态不只靠颜色，界面放大 200% 仍可完成操作。
- 报错用常规 Toast，展示后端返回的提示语（Demo 用预置文案即可）。
- 患者清单展示 **数据刷新时间**（该批计算完成时间）。

规则编辑采用左侧逻辑树与右侧参数面板；工作台采用顶部规模数字、左侧待办、右侧开放摘要和底部模块入口。列表统一使用浅灰页面、白色内容区、青蓝色主操作，正文约 14 px、次要文字不小于 12 px，状态含文字。

---

## 1. 产品定位与边界

APTS 是全院通用的 **患者识别与运营编排平台**。对外口径：「重点患者识别与连续性管理」。底层能力是标签画像，中医／专科／活动都是用通用模块配出来的样例，不进导航、不做专项页。

**拥有：** 标签定义与计算、周期性自动识别、人工复核、主动人群治理、数据集交付与标签订阅的业务授权、底座数据的指标化配置。

**不拥有：** 短信／电话等触达执行；预约、加号、住院、随访办理；接口运行、调用日志、推送失败与重试；CDR／时序库采集治理；必须逐条闭环的实时临床告警或临床决策支持；医学规则／模型有效性审定。

界面必须区分：

- 「授权已启用」≠ 对方已查询、接收或使用数据；
- 更不能写成患者已被触达或医疗资源已落实。

---

## 2. 术语

业务术语采用下列口径，配套领域词汇表提供完整定义：

| 用 | 不用 |
|---|---|
| 识别中心、待确认识别批次、复核 | 预警中心、预警任务、标签候选 |
| 人群快照、动态人群 | 标签人群、开放用现成名单 |
| 数据开放配置、授权已启用 | API、交付成功、开放接口 |
| 标签订阅（绑定动态人群） | 人群订阅（第三种开放类型）、标签原料通道 |
| 患者画像（上下文证据页） | 一人一档、患者档案 |
| 功能权限、标签使用授权、患者范围 | 把三者混成「权限」 |
| 数据刷新时间 | 标签规则更新时间、源数据截止时间 |

---

## 3. 建设分期

| 分期 | 建设内容 | 本次 Demo |
|---|---|---|
| **P0** | 数据集／指标库配置；标签定义、试算、发布、启停；固定周期自动识别与按范围确认；主动圈选动态人群／快照；数据开放（快照交付、动态人群订阅）；门户功能权限接入；标签使用授权；全院患者范围 | **全部可操作**（授权矩阵占位，范围固定全院） |
| **P1** | 多院区患者范围（全院可纳入无院区就诊建档对象）；专用模型治理；订阅增量推送／更新通知后拉取；可复用规则／实施模板 | 不实现；Spec 只留后续设计依赖 |
| **P2** | 内部标签使用与管理分析；后台审计；按场景数据留存与自动清理 | 运营分析入口显示「模块开发中」；无审计页、无清理 |

P0 标签生产只用规则逻辑树。底座已有的模型输出当普通指标引用。APTS 不在 P0 做模型注册、版本或审批。

---

## 4. 角色、职责与权限

身份、菜单和按钮由 **统一门户** 提供。APTS 按门户角色叠加 **标签使用授权**（白名单）和 **患者范围**。医院岗位名称不写成系统角色。

涉及标签实时使用的动作，要求同一个门户角色同时具备按钮、所用标签及患者范围，多个角色按完整授权取并集。快照查看／导出只校验按钮与患者范围，来源标签的证据另校验标签白名单；来源标签删除不阻止消费固定快照。Demo 默认一个全权限演示账号，不实现角色组合配置。

复合标签授权不沿引用扩大白名单：有复合标签 C 即可用 C 圈选／识别／开放；证据树可展示被引用基础逻辑的贡献，但不能把基础标签 A／B 当作已授权对象去圈选或跳独立命中清单。

### 4.1 职责模板（门户里组合，Demo 用演示账号走通）

| 职责 | 主要操作 |
|---|---|
| 标签维护人员 | 标签增删改、试算、发布、启停、自动识别开关／周期 |
| 运营人员 | 识别中心复核、人群圈选、查看画像 |
| 数据开放操作人员 | 创建、编辑、启用、暂停、恢复和导出；由门户按按钮赋权，可与其他职责组合 |
| APTS 业务管理员 | 标签使用授权、患者范围、已对接系统；不默认获得患者明细权 |
| 管理人员 | 只读汇总，不看患者明细 |

P0 患者范围固定 **全院** = 各院区就诊并集，不含无院区就诊的建档对象。P0 Demo 不提供范围切换。确认按患者范围各自落快照；P0 只有全院这一份。

标签及数据开放不设审批流程，不设审核岗位、待审核、驳回、重提或重审步骤。信息管理人员通常维护配置，但岗位名称不限制按钮赋权。不设敏感标签分级。

按钮权限分别接入门户：标签新建／编辑／试算／发布／停用／恢复／删除／自动识别配置；识别复核确认；人群新建／编辑条件／刷新／生成快照；开放新建／编辑／启用／暂停／恢复／导出；指标及已对接系统维护。无相应权限不显示可执行按钮，动作执行时仍校验权限与对象状态。编辑权限允许保存已发布标签的生效修改，不要求再次发布，不产生待发布副本；首次发布仍由发布按钮控制。

---

## 5. 领域对象与关系

```
数据底座（CDR／时序库）
  └── APTS 数据集（预置、只读）→ 指标库（绑定／衍生）→ 标签定义（基础／复合）
        ├── 试算（不落库）
        ├── 自动识别配置 → 计算批次 → 命中清单 → 复核清单（某患者范围视图）
        │                              └── 确认 → 人群快照
        └── 主动圈选（纳入／排除已发布标签）
              ├── 动态人群（收藏条件，可刷新、可被标签订阅绑定）
              └── 确认 → 人群快照 → 数据集交付
```

业务计算结果只保存命中患者；虚拟底座仍保留所有患者和观察供后续计算。不生成未命中、未知、未检查患者的业务清单。不允许手工给患者加标签；复核只能移除、不能添加。

计算批次、命中清单、触发时证据在保存后不因规则修改而改写。每个形成业务结果的计算同时保存只读“计算时规则说明”，记录来源计算对象和当时实际使用的条件：自动识别记录来源标签名称、标识及完整逻辑；主动圈选确认与动态人群刷新记录当时纳入／排除关系、其中各标签名称与标识及展开的逻辑，已保存人群另记录人群名称与标识。未保存的主动圈选不要求先创建动态人群对象。条件说明包含比较符与阈值、时间窗口／锚点、观察粒度及相关指标取值定义，与当次证据共同解释患者为何进入结果。订阅结果同样保留订阅及绑定动态人群标识、当时实际使用的纳入／排除条件说明，不因此转成内部计算批次或向外增加字段。历史页面从这份说明读取，不再套用当前标签规则；它附属于计算结果，不提供标签版本列表、回滚或恢复规则功能。人群快照通过来源批次继承解释，派生快照继续保留来源。复核时用当前数据 + 当前规则重算校验，仅在明确不命中时提示「当前已不满足」。

---

## 6. 核心流程与状态机

### 6.1 标签

草稿 → 已发布 → 人工停用；人工停用可恢复为已发布。草稿可保存未完成字段，发布／已发布保存修改必须通过完整校验。已发布保存修改即成为后续计算使用的当前逻辑；已启动计算使用启动时读取的完整逻辑算完，旧批次与证据不改写。无历史版本、无回滚。

未引用草稿可删除；已发布标签须先人工停用再删除，有依赖不阻止删除。删除前确认框列出受影响复合规则、识别配置、动态人群和订阅，并说明历史永久保留。已删除是终态，不提供恢复；稳定标识不复用。当前列表默认不含已删除标签，可筛选只读查看。

删除立即停止该标签及传递依赖的新计算和新增业务引用，停止返回尚未提供的计算或缓存结果；自动识别不再新增批次。复合规则保留失效引用并标明具体已删除标签，不能跳过该条件继续计算。动态人群、订阅与当前复核计算停止更新并提示失效原因，不能把失效解释为零命中或当前已不满足。用户可按正常编辑权限显式修复依赖，保存有效条件后后续允许计算才可恢复；不自动替换已删除标签。

历史快照、计算批次、命中与触发证据、复核记录、交付记录和已形成订阅结果作为历史业务记录永久保留，显示原标签名称、稳定标识及「历史标签／已删除标签」，不依赖来源标签仍存在。历史读取仍遵守当前功能权限、患者范围和标签证据权限；保留记录不等于扩大访问权。旧快照和已经形成的固定交付不因标签删除变化。已确认历史快照仍可查看、直接导出及新建数据集交付，来源标签已删除不构成阻断；这些动作只消费固定快照，不重新引用该标签计算。历史记录不能冒充当前计算结果。

指标或基础标签停用时叠加依赖不可用，停止新计算和未返回结果；恢复后自动解除该层不可用，独立人工停用保持有效。删除的依赖不会因其他指标恢复而自动解除。

### 6.2 自动识别批次（按患者范围）

周期到点生成新的不可改写批次。对当前工作范围：尚未确认则待确认；确认后该范围对该批次立即只读；保留人数大于0时生成快照，全移除时只保留零人确认记录。新批次到来后，上一批次对所有范围转为只读历史。范围内 0 个命中患者则不出现待确认。P0 不催办、不逾期升级；最新结果非空时发一次「识别结果已更新」。

### 6.3 数据开放配置

配置按草稿、授权已启用、已暂停、已到期展示，另显示全部依赖失效原因。配置状态与依赖可用性分开记录，任何阻断未解除都不可开放。当前产品不设计数据开放审批，也不预设后续分期一定增加审批。

| 前置与动作 | 结果与限制 |
|---|---|
| 新建／保存草稿 | 仅保存配置，不计算、不下载、不启动通道交付；草稿可不完整 |
| 草稿点击启用 | 校验完整字段、按钮权限、对象和系统可用性；默认立即生效，不预约生效 |
| 已启用点击暂停 | 立即停止新查询、下载和推送，未返回结果也停止提供；历史记录保留 |
| 人工暂停点击恢复 | 重新校验全部条件后恢复；依赖恢复不会代替此动作 |
| 达到截止时间 | 自动已到期并停止开放；修改为未来截止或长期有效后仍须点击恢复 |
| 保存名称、用途、说明、方式、周期或有效期 | 有编辑权限即可直接修改；启用后类型、使用方／已对接系统、绑定对象固定，需要更换则新建配置；方式须仍由同一系统支持，不能借改方式换使用方 |
| 已启用时缩短期限至当前或过去 | 立即到期；草稿启用／暂停恢复的截止时间必须在当前之后 |
| 已对接系统停用 | 关联已启用配置转为已暂停，原因记录“系统停用”；原已暂停配置追加原因，原草稿／已到期保持原状态。系统恢复后暂停仍保留，原因显示“系统已恢复，待人工恢复”；点击恢复并通过全部校验才启用。不影响未绑定该系统的直接导出 |
| 条件中任一标签停用／删除 | 整条订阅计算及返回被阻断，不能忽略纳入或排除条件；停用恢复自动解除依赖层，删除须显式修复依赖 |

状态显示优先级：从未启用的配置显示草稿；已启用过且截止时间已到、或到期后改期限但尚未点击恢复，均显示已到期（后者附“期限已调整，待恢复”说明）；其余有人工或系统暂停原因显示已暂停；其余显示授权已启用。标签依赖不可用另列原因并阻止开放，不改变配置自身状态。系统已恢复但待人工恢复是已暂停的原因说明，不增加第五种状态；解除标签依赖也不能清除该暂停。已到期配置修改期限后保留待恢复要求，点击恢复才清除该要求。

有效期选择长期有效（默认）或指定截止时间，时间精确到分钟，按医院时区计算，截止时刻不再有效。启用时间自动记录。查询最小间隔与推送周期由已对接系统分别声明，前台只能选择支持方式／周期；前者控制重算频率，后者控制定时全量，不是有效期。

修改动态人群条件时显示关联订阅名称、状态及影响，不复制条件到开放表单，不暂停重审。普通条件、规则或周期修改在下一次允许计算使用；已发出结果不改写，间隔内可复用最近成功结果并显示原计算时间。停用、删除、到期和撤权立即阻断，不能继续返回缓存。

### 6.4 纳入／排除（人群与订阅共用）

纳入 = 必须同时命中全部纳入标签；排除 = 命中任一排除标签即去掉。无且／或开关。或关系先做成复合标签再纳入。单标签纳入、排除为空合法。

---

## 7. 规则语义（P0 Demo 必须可配置）

- 所有判断只使用数据底座已接入范围。
- 统一逻辑树。基础标签用指标条件；复合标签可引用 **已发布基础标签** 的当前逻辑，并混合新指标条件、分组、且／或。禁止复合标签互相引用，禁止整组取反。
- 判断方式随指标元数据点亮：普通属性直接比较；记录可最新一次／存在／累计次数／最近 N 次每次均满足／次数为 0；数值序列可聚合与变化比较。只有一种方式时可默认、少让用户选。
- 记录条件组：先在一次观察内评价组合条件，再做存在／次数／最近 N 次。无法判断的观察不计次；明确正常的观察参与排序并打断连续异常。
- 同次就诊是额外限定，默认不要求。
- 「无记录」= 指定已接入数据、指定窗口内次数为 0。查询失败走报错，不当零记录。
- 计算类指标优先用底座已有结果；缺的在指标库配衍生（算术、日期差、窗口内计数／求和／平均／最大／最小）。逻辑树不维护公式。
- 医学模型、图像／波形、复杂节律由底座提供结果，规则只引用。

规则编辑 UI：**左侧树显示摘要，点选后右侧改参数（左树右参数）**。树上只显示包括时间／次数在内的摘要；选中条件或记录组后，统一在右侧编辑参数。空组、空规则不能发布。

试算：只出命中人数和脱敏样例，不生成批次、不出现证据链、不出现「当前已不满足」。

### 7.1 能力、参数与校验

下表定义 P0 规则与指标的表达边界；比较、时间、计次、逐次复用、首次、变化及衍生配置须可操作。依赖底座的持续／复杂医学计算只引用明确的输入结果，不要求实现者自行开发医学算法。规则编辑面是标签详情的左树右参数，计算公式维护面是指标库。选值后即时更新树摘要，保存值、摘要和重新打开的控件必须一致。默认只用于减少选择，用户仍能选择适用能力。缺失不补成零或正常；明确不满足、无法判断和查询失败在计算内部区分，不另建未知患者清单。

| 能力与编辑面 | 参数与默认 | 元数据前提及限制 | 正反例 |
|---|---|---|---|
| 直接比较：规则 | 数值 =、≠、>、≥、<、≤、闭区间；文本／枚举 =、≠、属于、不属于；日期 =、早于、晚于、闭区间；布尔 是／否。数值默认 ≥，其他默认 =；阈值必填 | 值类型和值域来自指标；枚举多选非空，区间起点不得大于终点；空值无法判断，不用 ≠ 隐式认定正常 | 年龄 ≥18 命中 18、不命中 17；科室只能 =眼科，不能 ≥眼科 |
| 最新值：规则 | 时间窗口、比较符、阈值；先取最新有效记录再判断 | 按业务时间排序，撤销／无效记录排除；并列时间按指标声明解决，不能随机挑选 | 18→7，阈值 ≥10 不命中；不能先筛出 18 再说最新命中 |
| 时间窗口：规则 | 默认近 90 天、相对计算时点；可固定起止日期或事件前后偏移，天数为非负整数 | 起止包含边界，固定结束日期含全天；医院时区；源记录须在计算时点已发生且已入库 | 恰在边界内，晚于计算时点的事件不能作证据 |
| 事件锚点：规则 | 从已接入事件选类型；首次／最近一次／任一次，默认最近一次；前后偏移允许 0 | 锚点只取计算时点已发生事件，无锚点不命中；任一次逐锚点评价、至少一次满足，不能拼接不同锚点的组合证据；跨窗口计次同一观察去重 | 两次出院分别 8月1日、9月1日；8月15日评分落在首次出院后30天，不能算作最近出院后的评分 |
| 存在／计次／零记录：规则 | 窗口、观察粒度、观察条件、次数比较符及整数阈值；存在即次数 ≥1 | 先按记录／采集／就诊／日期声明去重关联，再评价每次观察；次数统计满足条件的观察；零记录限定已接入范围；失败不等于 0 | 同次采集两行不计两次；查询成功无记录可为 0，查询失败报错 |
| 最近 N 次每次满足：规则 | 窗口、观察粒度、N（正整数，默认3）、内部条件 | 先取最近 N 次可判断观察，正常与异常均参与；未知观察跳过，不足 N 不命中 | 异常→缺项→异常→异常满足 N=3；异常→正常→异常不满足 |
| 观察内组合：规则 | 记录条件组，粒度、关联键由元数据提供；内部且／或、基础逻辑引用 | 固定同一患者；同次就诊须显式限定；基础逻辑须支持逐次评价且时间／关联兼容，否则禁用并解释；不能将患者级最新逻辑悄悄转成逐条逻辑 | 同一次观察的评分高且症状存在可命中；跨两次观察各满足一项不能拼接 |
| 首次异常：规则 | 异常条件、发生时间窗口 | 搜索底座已有历史中首次满足异常的有效观察，再看是否在窗口；不是窗口内首次，也不证明医学新发 | 7月已有异常，9月再异常，不满足“9月首次异常”；首次检查即异常可满足 |
| 较前变化：规则 | 默认最近两次可比有效记录；绝对变化=本次−前次；变化率=(本次−前次)/前次绝对值×100%；比较符和阈值必填；分类状态用前状态→后状态 | 不足两条不命中；变化率前次为0无法判断；单位及口径必须可比，转换关系由指标声明，不猜测；其他基准仅在元数据明确支持时显示 | 10→15 为 +5、+50%；0→5 绝对变化为5，变化率无法判断；不能表示为0或无穷大 |
| 衍生算术／日期差：指标库 | 输入指标、取值方式、明确公式、结果单位；算术 +−×÷ 与括号，日期差指定起止与单位 | 优先复用底座结果；字段输入须同患者并按声明关联，无关联不得笛卡尔拼接；除0或缺输入无法判断；日期差不取绝对值掩盖倒序 | 体重60kg÷身高1.5m平方=26.67kg/m²；无身高不产出BMI |
| 衍生窗口统计：指标库 | 指标、窗口、观察粒度、可选过滤；计数／和／均值／最大／最小；输出作为可复用指标引用 | 统计前去重并排除无效值；空集合计数为0，其他统计无法判断；可明确“存在记录且合计为0”，不得混同缺数据 | 10、20均值15；空记录不能声称平均0 |
| 连续日期：规则 | 时间窗口、连续天数N（正整数，默认3）、每日判断“当天存在一次满足／当天全部有效观察满足”（默认存在一次）、观察条件 | 按医院自然日分组，先判断每天是否满足，再在窗口内找连续N个相邻满足日；每天至少一条可判断观察，无可判断观察的日期打断连续，不跳日；无法判断观察不算正常或满足 | 同一天评分12和7：存在一次≥10为满足，全部有效观察≥10为不满足；中间一天无可判断观察则不能跨日凑足N天 |
| 持续／事件顺序：规则及指标库 | 按适用指标声明有效时段、时长单位及先后事件；普通扫描周期不作临床参数 | 持续须底座有效区间或显式覆盖口径，重叠区间合并；复杂波动／节律指标由底座提供；不支持时说明缺少何种输入 | 连续三次异常不等于持续三天 |

“且”中有明确不满足则不满足，全部满足才满足；“或”中有明确满足即满足，全部不满足才不满足；其余为无法判断。最终只有明确满足才命中。元数据须声明有效性、去重、更正、并列记录处理、业务时间、单位、可比性及适用判断方式；缺少对应必要声明时禁止启用相关方式，不自行决定源数据治理。

### 7.2 独立规则验收数据

以下是规则试算专用虚拟观察，不混入主故事患者与首页规模。在演示辅助区提供“规则能力样例”，将配置载入独立的临时试算副本并使用下表观察，不覆盖业务标签或已保存数据；退出即回业务数据。可编辑参数并重新计算。每条观察包含独立患者、观察标识、业务时间及入库时间，入库默认业务时间后1分钟，撤销标识默认否。所有日期为2026年；未写时刻的观察统一09:00，计算日统一11:00；未写日期的成对观察取9月1日及2日、9月3日计算。

| 样例 | 输入与计算时点 | 预期 |
|---|---|---|
| 最近 N 次有效观察 | 9月1日至4日各09:00评分12、缺失、13、14；9月5日计算，N=3、≥10 | 命中；将9月3日改成7则不命中；N=4不足次数 |
| 连续日期 | 9月1日09:00评分12；2日09:00评分13、15:00评分7；3日09:00评分14；4日11:00计算固定9月1日至3日，连续3天、评分≥10 | 默认当天存在一次满足：命中；改为当天全部有效观察满足：不命中；移除2日全部观察或全部改为无法判断：两种均不命中，不能跨缺日连接 |
| 首次异常 | 7月1日12、9月1日14；9月7日算9月首次、阈值10 | 不命中；移除7月记录后命中；历史证据不随试算改写 |
| 变化 | 两次评分10、15；第二次入库后计算 | 绝对差5，变化率50%；前次改0则变化率无法判断 |
| 观察组合 | 观察A评分12、症状否；B评分7、症状是 | “同次评分≥10且症状是”不命中；B评分改12则命中 |
| 事件窗口 | 出院8月1日及9月1日00:00，评分8月15日12；9月7日计算，各出院后0至30天 | 首次／任一次命中，最近一次不命中；无出院锚点不命中 |
| 固定窗口 | 9月1日00:00及9月7日23:59各一条有效记录，9月8日计算 | 9月1日至7日计数2，删除一条则1；模拟失败不等于0 |

---

## 8. 信息架构与页面

一级导航（门户功能权限控制可见性）：

**工作台 · 标签中心 · 识别中心 · 人群管理 · 数据开放 · 运营分析 · 系统管理**

系统管理子页：数据集、指标库、已对接系统、标签使用授权（Demo 占位）、患者范围（P0 只读全院）。审计日志为 P2，Demo 不出现可操作入口。

患者画像不是一级菜单，无全院患者检索，只从识别结果或人群成员进入。

「预警」「服务策略」不进入本产品导航。

建议路由（Demo 可等价实现）：

| 路由 | 页面 |
|---|---|
| `/` | 工作台 |
| `/tags`、`/tags/:id` | 标签列表／详情 |
| `/recognition`、`/recognition/:tagId/batches/:batchId` | 识别概览／批次患者与证据 |
| `/cohorts`、`/cohorts/new`、`/cohorts/:id` | 人群列表／新建／详情 |
| `/open`、`/open/new`、`/open/:id` | 数据开放 |
| `/analytics` | P2 占位 |
| `/admin/datasets`、`/admin/metrics`、`/admin/systems`、`/admin/auth`、`/admin/scopes` | 系统管理 |
| `/patients/:id?from=` | 患者画像（带入来源） |

---

## 9. 逐模块 P0 需求

每条需求的角色默认见第 4 章；实时标签计算按同一角色的功能权限、标签白名单和患者范围共同校验。已确认快照的查看与交付按功能权限及患者范围校验，标签证据另外校验白名单，不能因来源标签删除而禁止快照导出。P0 Demo 用评审演示账号，范围固定全院。

样例数据（生成 Demo 必须预置，可在界面改配置但不能改医学含义宣传）：

**数据集（只读）**

| 名称 | 来源 | 表名 | 粒度 | 关键字段 |
|---|---|---|---|---|
| 患者基本信息 | CDR | cdr_patient | 患者 | patient_id, name, age, sex, phone |
| 门诊就诊记录 | CDR | cdr_encounter | 就诊 | patient_id, encounter_id, department, visit_time, diagnosis |
| 眼表评估记录 | 时序库 | ts_eye_assessment | 采集 | patient_id, encounter_id, observation_id, observed_at, score, symptom |

**指标库**

| 指标 | 类型 | 绑定／公式 | 判断方式 |
|---|---|---|---|
| 患者年龄 | 字段绑定 | cdr_patient.age | 直接比较 |
| 就诊科室 | 字段绑定 | cdr_encounter.department | 最新／存在／次数／零记录 |
| 就诊时间 | 字段绑定 | cdr_encounter.visit_time | 最新／存在 |
| 眼表评分 | 字段绑定 | ts_eye_assessment.score | 最新／存在／累计次数／最近 N 次每次满足／连续日期／首次异常／较前变化 |
| 不适症状 | 字段绑定 | ts_eye_assessment.symptom | 最新／存在 |
| 近 30 天平均眼表评分 | 衍生 | 平均值(眼表评分, 30 天) | 直接比较 |

**标签**

| 标签 | 类型 | 规则摘要 | 自动识别 |
|---|---|---|---|
| 眼表评分偏高 | 基础 | 近 90 天最新一次眼表评分 ≥ 10 | 关 |
| 成年患者 | 基础 | 年龄 ≥ 18 | 关 |
| 眼科复诊运营筛选 | 复合 | 引用「眼表评分偏高」且 引用「成年患者」且 近 90 天存在眼科就诊 | 开 · 每 7 天 |

**患者（主故事，眼科复诊运营筛选 · 批次 B-20260907-0800，刷新时间 2026-09-07 08:00）**

| ID | 姓名 | 性别 | 年龄 | 就诊 | 科室 | 触发时评分 | 当前评分 | 备注 |
|---|---|---|---|---|---|---|---|---|
| P-DEMO-001 | 张敏 | 女 | 56 | V-20260820-011 | 眼科 | 16 | 14 | 可确认 |
| P-DEMO-002 | 李强 | 男 | 61 | V-20260820-018 | 眼科 | 18 | 7 | **当前已不满足** |
| P-DEMO-003 | 王芳 | 女 | 48 | V-20260821-004 | 眼科 | 15 | 15 | 可确认 |
| P-DEMO-004 | 赵洋 | 男 | 52 | V-20260822-009 | 眼科 | 12 | 12 | 可确认 |
| P-DEMO-005 | 陈晨 | 女 | 44 | V-20260822-021 | 眼科 | 11 | 11 | 可确认 |
| P-DEMO-006 | 周宁 | 男 | 67 | V-20260824-002 | 眼科 | 13 | 13 | 可确认 |

试算脱敏样例使用当前实际命中患者的脱敏姓名，不能固定展示已不命中的李强。另预置 P-DEMO-007 林晓（女，35 岁，2026-08-25 全科就诊，评分 5）和 P-DEMO-008 吴平（男，40 岁，2026-08-25 全科就诊，无评分记录），供健康活动圈选使用。

**已对接系统（演示登记）：**

| 名称／编码 | 默认使用方 | 状态 | 支持方式 | 周期声明 | 底座通道引用 |
|---|---|---|---|---|---|
| 患者服务系统／SYS-PATIENT | 眼科随访组 | 可用 | 一次性快照通道交付、按需查询、定时全量推送 | 查询最小间隔1天；推送可选每7天 | DEMO-CHANNEL-001 |
| 旧随访接口／SYS-LEGACY | 随访管理组 | 停用 | 一次性快照通道交付、定时全量推送 | 不支持按需查询；推送可选每7天 | DEMO-CHANNEL-002 |

两个登记项责任组织均为信息管理部门，业务／技术联系人分别使用虚拟文本“演示业务联系人”“演示技术联系人”，最后校验时间为2026-09-01 09:00。通道引用仅为只读演示标识，不是URL，不发起网络请求。

**初始开放配置为空。** 用户确认快照后创建「眼科复诊名单交付」（直接导出，使用方眼科随访组，用途诊后复诊提醒）；健康活动圈选订阅在用户保存动态人群并新建开放后出现。不能预置没有来源快照的交付。

---

### 演示字典与补充输入

以下字典固定用于Demo，不建设字典维护页面。可新增技术标识，不自行增加分类含义或医学阈值。

| 项目 | 固定值或规则 |
|---|---|
| 标签分类 | 基本属性、就诊特征、评估结果、组合筛选；成年患者归基本属性，眼表评分偏高归评估结果，眼科复诊运营筛选归组合筛选 |
| 指标目录 | 患者属性、就诊记录、眼表评估、衍生指标；按输入归类 |
| 责任组织 | ORG-INFO 信息管理部门；ORG-EYE 眼科；默认信息管理部门 |
| 使用方 | 眼科随访组、随访管理组、健康活动组织组；为自由文本示例，不另做使用方管理模块 |
| 事件类型 | 门诊就诊、出院；前者来自就诊记录，后者在规则样例提供患者标识、事件标识、发生时间和入库时间 |
| 算术样例 | 独立患者RULE-BMI，同次采集体重60kg、身高1.5m；公式体重÷(身高×身高)，结果26.67kg/m²；身高改为缺失或0则无法判断 |
| 日期差样例 | 独立患者RULE-DATE，起点2026-09-01 09:00，终点09-03 09:00，结果2天；交换起止得−2天，缺任一端点无法判断 |
| 窗口统计样例 | 独立患者RULE-STAT，9月1日及2日09:00各评分10、20，9月3日11:00计算近30天；计数2、和30、均值15、最大20、最小10；清空输入后计数0，其余无法判断 |

补充输入放独立规则样例区，不改变主故事规模；使用与主故事相同的计算与指标配置能力。虚拟观察的默认入库时间为业务时间后1分钟。

### 9.0 共享事件与演示时钟

全平台共用患者、就诊、观察、标签、批次、复核、快照、人群、开放配置与结果对象。所有页面统计从这些对象推导，不各自预写人数或状态。医院时区 Asia/Shanghai，默认演示复核时点 2026-09-07 11:00；通过模拟时钟推进计算、到期和推送，刷新页面可重置。

- 上表六人的就诊业务时间均为就诊 ID 日期 09:00；触发评分观察时间为各自就诊日期 09:30，患者、就诊、采集标识一一关联。观察标识使用 O-患者尾号-日期-序号，业务时间与入库时间均明确，入库默认晚 1 分钟。
- 初始批次 B-20260907-0800 于 08:00 计算完成，六人最新评分分别 16、18、15、12、11、13，均成年且近 90 天有眼科就诊，保存六人命中及对应触发证据。
- 09-07 09:00 张敏新增评分 14，10:00 李强新增评分 7，均各自形成新观察并在 1 分钟后入库。11:00 复核李强明确不命中；其他人不显示“仍满足”徽标。批次证据不能混入这两条计算后观察。
- 11:00 当前规则试算命中五人；阈值改为 13 后当前命中张敏、王芳、周宁三人，旧六人批次不变。恢复阈值 10 后新批次命中五人。
- 11:00 健康活动圈选纳入成年、排除复诊筛选，结果为李强、林晓、吴平三人。移除只影响本次待确认快照名单；选择保存动态人群时保存条件及未经人工移除的完整计算结果，当下即显示三人，不必等刷新后恢复。选择保存快照才保存移除后的人数。
- 初始只有上述一个识别批次；历史切换通过“产生新批次”演示，不预置缺少证据的历史批次。每次计算只读业务时间、入库时间均不晚于计算时点的记录。
- 确认时自动生成唯一快照标识、来源批次、确认人、确认时间及成员摘要。移除李强后为五人，保留为六人，全部移除则确认完成、零保留、不生成快照；工作台不再显示该批次待办。
- 主故事中的标签、人群、识别结果及开放配置默认责任组织为信息管理部门；患者对象不设置责任组织字段，演示经办人为 DEMO-OP。联系电话仅用明确虚构文本 DEMO-TEL-001 至 008，李强和吴平留空；界面注明非真实号码。虚拟底座中 name、phone 属于 cdr_patient，symptom 属于 ts_eye_assessment，不能绑定到错误数据集。
- 主故事指标预置元数据：年龄为整数患者属性；科室为就诊枚举；评分为数值、单位分、观察粒度采集，以 observed_at 排序；症状为布尔，同 observation_id 关联。撤销记录不参与，修订以同观察最新入库项为准；不同比较对象如单位／粒度不兼容则禁用相关方式。评分允许最新、存在、计次、最近N次、连续日期、首次及变化；普通年龄只做直接比较。主故事不产生同一业务时刻冲突，另用能力样例验证对应限制。
- 演示目录、事件与基础衍生输入使用本文演示字典；实现者只补充唯一技术标识及存储结构，不改变主故事八人的事实和预期人数。元数据跟随虚拟字段预置，不要求用户在演示时手工治理数据。
- 模拟查询失败仅显示 Toast，不生成新批次、空名单或结果版本；原有历史对象保留。模拟成功零人订阅生成新结果版本，整体结果元信息仍存在。

### 9.1 工作台 `WB`

布局：顶栏当前规模数字 → 左待办摘要、右数据开放摘要 → 下各模块入口。数字只表示当前规模，无趋势、人次、效果。事项只跳转，首页不复核、不启停开放、不导出。

| ID | 操作 | 主流程 | 空态／异常 | Demo |
|---|---|---|---|---|
| WB-01 | 查看规模数字 | 展示待确认识别批次／人数、授权已启用、动态人群／快照、已发布标签 | 全 0 时仍展示数字 0 | 完整 |
| WB-02 | 待办：识别结果已更新 | 进入识别中心对应标签最新批次 | 最新批次已确认或 0 人则不出现此待办 | 完整 |
| WB-03 | 开放摘要跳转 | 点配置名称进入对应详情，点状态进入筛选列表 | 无配置不伪造待办 | 完整 |
| WB-04 | 开放摘要 | 列出草稿／授权已启用／已暂停／已到期及依赖原因；旁注不表示对方已收到 | 无配置时空列表文案 | 完整 |
| WB-05 | 模块入口 | 打开标签／识别／人群／数据开放 | 无权限的模块不出现 | 完整 |
| WB-06 | 运营分析入口 | 进入占位页「模块开发中」 | — | 完整（占位页） |
| WB-07 | 禁止首页处理 | 无复核、开放启停、导出按钮 | — | 完整 |

初始规模：待确认识别批次 1、待确认人数 6、开放配置 0、动态人群 0、快照 0、已发布标签 3；所有数字由共享对象推导。无审批待办，无异常时不显示红色待处理。

---

### 9.2 标签中心 `TAG`

列表 + 单页详情（不分 Tab）：基本信息 → 规则（左树右参数）→ 试算 → 自动识别开关与周期。

| ID | 操作 | 主流程 | 空态／异常 | Demo |
|---|---|---|---|---|
| TAG-01 | 列表筛选 | 按类型、状态筛选；显示可用性 | 空列表引导新建 | 完整 |
| TAG-02 | 新建基础标签 | 选类型基础，编指标条件，存草稿 | 无条件不能发布 | 完整 |
| TAG-03 | 新建复合标签 | 可引用已发布基础标签（只读展开）+ 新条件 | 不能引用复合标签 | 完整 |
| TAG-04 | 编辑基本信息 | 改名称、分类、责任组织与可选处置建议；类型创建后不可改 | 允许重名，显示稳定标识区分 | 完整 |
| TAG-05 | 规则编辑 | 树摘要，点选右侧改参数；可分组、且／或、同次就诊、记录条件组 | 空组不能发布或保存为生效修改 | 完整 |
| TAG-06 | 保存草稿／发布 | 已发布有编辑权限可保存生效修改，无待发布副本和版本历史 | 校验失败明确提示 | 完整 |
| TAG-07 | 启停 | 人工停用后新计算停止；已有清单保留 | — | 完整 |
| TAG-08 | 试算 | 命中人数 + 脱敏样例 | 不可用时不能当零命中，提示原因 | 完整 |
| TAG-09 | 自动识别开关／周期 | 只对可用已发布标签；改周期后识别概览同步；本页不跑识别 | 未发布不能开 | 完整 |
| TAG-10 | 依赖不可用 | 指标或基础标签停用后，本标签及引用它的复合标签提示原因 | 恢复依赖后自动恢复；人工停用保持 | 完整 |
| TAG-11 | 删除标签 | 未引用草稿可删，已发布先停用；确认影响后删除 | 已发布且未人工停用的标签不能直接删，失效依赖不可继续算；历史永久保留 | 完整 |

---

### 9.3 识别中心 `REC`

下钻：概览 → 标签批次 → 左清单右同屏证据。改开关／周期回标签中心。

| ID | 操作 | 主流程 | 空态／异常 | Demo |
|---|---|---|---|---|
| REC-01 | 概览 | 列出已开启自动识别的标签及已停止的历史入口：周期、最近扫描、人数、确认状态；删除后只读历史入口标明原因，不计入当前自动识别标签数 | 无已开启自动识别的标签且无历史批次才显示空态 | 完整 |
| REC-02 | 打开最新批次 | 左侧显示当前批次实际成员（初始6人），右侧画像；可移除／确认 | 0 命中不出现待确认 | 完整 |
| REC-03 | 当前已不满足 | 李强证据区提示；触发时证据仍在；不自动移除、不阻止确认 | 无提示不表示已验证仍满足 | 完整 |
| REC-04 | 移除 | 原因三档：当前已不满足／不应纳入本次运营人群／其他（可填说明）；不能添加 | 原始命中说明保留 | 完整 |
| REC-05 | 确认成快照 | 剩余成员生成快照进入人群列表；本范围本批次立即只读 | 全移除后按 0 人规则：不生成空快照，批次仍只读 | 完整 |
| REC-06 | 历史批次 | 只读，无移除／确认 | 新批次到来后上一批变历史 | 完整 |
| REC-07 | 模拟下一周期 | Demo 提供「产生新批次」以便评审只读切换 | 失败 Toast | 完整 |
| REC-08 | 站内提醒 | 非空最新结果一次「识别结果已更新」 | 空结果只记执行时间 | 完整（工作台卡片即可） |

移除原因写入复核记录（时间、人、结果、原因、当时当前复核状态摘要）。识别中心确认后不能在本模块撤回快照成员；再改人到人群管理，从原快照移除成员后生成新快照，原件不变。

---

### 9.4 人群管理 `COH`

一个已保存列表 + 「新建人群」。圈选只选已发布标签，不在此页写指标。主动路径 **不** 出现「当前已不满足」。中间检索不保存。

| ID | 操作 | 主流程 | 空态／异常 | Demo |
|---|---|---|---|---|
| COH-01 | 列表 | 含识别来源快照与主动保存对象；类型、来源、人数 | 空态引导新建 | 完整 |
| COH-02 | 新建圈选 | 纳入／排除已发布标签后检索 | 缺任一标签授权则不能检索，提示缺授权 | 完整 |
| COH-03 | 检索中移除 | 只影响本次待保存名单 | 无「当前已不满足」 | 完整 |
| COH-04 | 存动态人群 | 保存条件及对应的未经人工移除的完整计算结果，0人也可存 | 提示“保存条件，不保留本次人工移除”；结果不可用时只存条件，不冒充已有有效成员 | 完整 |
| COH-05 | 存快照 | 人数须 > 0 | 0 人按钮不可用并说明 | 完整 |
| COH-06 | 动态人群刷新 | 按当前条件重算；P0 手动；保留成功批次供查看 | 失败 Toast，不把失败显示成 0 人名单 | 完整 |
| COH-07 | 从动态人群确认快照 | 生成独立快照；动态人群仍在；移除不写回条件 | 0 人不能确认 | 完整 |
| COH-08 | 开放订阅 | 进入数据开放新建标签订阅并预填该动态人群 | 缺授权不能发起 | 完整 |
| COH-09 | 详情 | 类型、来源、纳入／排除、成员、相关批次、刷新时间 | — | 完整 |
| COH-10 | 从快照调整成员 | 以原件为起点移除，确认后生成新快照并记录来源 | 原件和旧交付不变，零人不生成 | 完整 |
| COH-11 | 编辑动态条件 | 展示关联订阅及影响，有编辑权限可保存 | 不重审、不改写旧结果，下一次计算用新条件 | 完整 |
| COH-12 | 历史快照导出入口 | 进入预填当前快照的直接导出配置；填写使用方用途、启用、下载 | 来源标签删除不阻止；仍校验导出按钮和患者范围 | 完整 |

保存动态人群不将人工移除作为条件或成员覆盖写入。已有成功结果须对应当前条件，保留原计算时间；条件已修改而尚未成功计算时，只保存条件，显示“尚未计算”，不沿用旧条件结果作为当前成员。保存时按钮旁显示“保存条件，不保留本次人工移除”；希望保留人工筛选名单时使用“保存快照”。

健康活动样例：名称「健康活动圈选」，纳入「成年患者」，排除「眼科复诊运营筛选」。

---

### 9.5 数据开放 `OPEN`

统一列表按类型和状态筛选，详情展示当前配置、可用性原因、创建／最近经办信息和固定交付记录，不展示审批记录或技术调用日志。

| ID | 操作 | 主流程 | 空态／异常 | Demo |
|---|---|---|---|---|
| OPEN-01 | 列表筛选 | 两类配置共用列表，查看状态与依赖原因 | 无配置引导新建 | 完整 |
| OPEN-02 | 新建数据集交付 | 只能绑定已确认快照 | 动态人群不可选，无快照引导确认 | 完整 |
| OPEN-03 | 直接导出方式 | 填使用方、用途，不强制选择系统 | 缺必填不能启用 | 完整 |
| OPEN-04 | 通道交付 | 选支持该方式的已对接系统 | 系统停用不可启用 | 完整 |
| OPEN-05 | 保存草稿 | 保存未启用配置 | 不完整可存，不能下载或计算 | 完整 |
| OPEN-06 | 启用 | 校验通过后授权已启用，记录启用时间 | 缺权限、无效对象、截止已过则阻止 | 完整 |
| OPEN-07 | 恢复 | 人工暂停或修改到期期限后手动恢复 | 系统／标签依赖未解除时不能恢复 | 完整 |
| OPEN-08 | 直接导出 | 下载真实虚拟 CSV，记录固定内容、来源、经办人、使用方、用途、时间 | 草稿／暂停／到期不可下载，不用 Toast 冒充文件 | 完整 |
| OPEN-09 | 新建标签订阅 | 绑定动态人群，选按需查询或定时全量，结果固定三字段 | 无动态人群引导创建，依赖不可用不能启用 | 完整 |
| OPEN-10 | 暂停／到期 | 立即停止下载、查询、推送和未返回结果 | 已发出结果不撤回 | 完整 |
| OPEN-11 | 修改开放配置 | 有编辑权限直接改用途、方式、周期、期限；下一次允许计算采用有效配置 | 启用后类型、使用方／系统及绑定对象锁定，无自由扩字段 | 完整 |
| OPEN-12 | 系统停用 | 全部关联开放停止，显示原因 | 系统恢复后配置须人工恢复 | 完整 |

订阅只使用动态人群当前条件现算，不使用人群页手动刷新名单，不落内部快照。纳入／排除任一标签不可用，整条停止计算与返回。多个原因同时存在时全部显示，不得恢复其中一项就误启用。Demo 提供模拟查询、推进至下一推送时点、模拟到期与计算失败入口；模拟操作生成可检查的患者标识、结果版本和计算完成时间，不宣称真实发送。

数据集交付在首次实际导出或模拟通道交付时形成固定内容；授权启用本身不表示交付已发生。直接导出的后续下载重用同一固定内容并记录下载时间，不能因当前患者数据变化生成不同文件。新快照需创建新开放配置。通道只模拟形成可交付固定结果，不模拟第三方接收成功。

---

### 9.6 运营分析 `ANL`

| ID | 操作 | 主流程 | Demo |
|---|---|---|---|
| ANL-01 | 打开模块 | 标题「运营分析」，正文「模块开发中」。P2 才做内部使用分析，见第 3 章 | 占位 |

---

### 9.7 系统管理 `ADM`

| ID | 操作 | 主流程 | 空态／异常 | Demo |
|---|---|---|---|---|
| ADM-01 | 数据集查看 | 切换预置表，看来源、患者／就诊／记录／时间关联、字段与样例 | 无新增改删停；无「配置为指标」 | 完整 |
| ADM-02 | 指标列表 | 目录、绑定、状态、引用数 | 空态引导新建 | 完整 |
| ADM-03 | 新建／编辑指标 | 弹层；直接绑定或衍生；选适用判断方式 | 名称空、无判断方式不能存 | 完整 |
| ADM-04 | 改绑定 | 保存前列出受影响标签；新绑定用于后续计算 | 类型／单位／粒度不兼容则阻止 | 完整 |
| ADM-05 | 停用／恢复指标 | 依赖标签不可用；恢复后自动解除依赖层 | 人工停用的标签保持停用 | 完整 |
| ADM-06 | 删除指标 | 无引用才可删 | 有引用阻止并列出去向 | 完整 |
| ADM-07 | 已对接系统 | 登记名称、编码、默认使用方、开放方式、状态、查询间隔／推送周期声明 | 无地址、密钥、日志 | 完整 |
| ADM-08 | 标签使用授权 | 仅显示“标签使用授权配置暂不演示”及默认演示账号说明，无可编辑矩阵 | — | **占位页** |
| ADM-09 | 患者范围 | 展示当前「全院」及不含无院区建档对象 | 无多院区编辑 | 只读说明 |

---

### 9.8 患者画像 `PAT`

| ID | 操作 | 主流程 | 空态／异常 | Demo |
|---|---|---|---|---|
| PAT-01 | 从识别进入 | 保留来源；可复核（仅最新待确认批次） | — | 完整 |
| PAT-02 | 从人群进入 | 只读，无复核按钮 | — | 完整 |
| PAT-03 | 身份与就诊 | 姓名、患者 ID、性别、年龄、就诊 ID、就诊科室 | — | 完整 |
| PAT-04 | 来源条件与触发证据 | 按进入来源显示标签逻辑或人群纳入／排除条件及相关证据；时间轴按业务时间 | 不展示全量病历 | 完整 |
| PAT-05 | 当前已不满足 | 仅明确不命中时出现 | 静默不表示已复核仍命中 | 完整 |
| PAT-06 | 其他获授权标签 | 放次级区域 | 未授权标签不出现 | 完整 |
| PAT-07 | 复合证据 | 可展开基础逻辑贡献；不能跳到未授权基础标签命中清单 | — | 完整 |

---

## 10. 外部系统与数据开放合同

线下完成技术对接后，后台登记 **已对接系统**。前台只做业务授权。 系统支持方式仅包含“一次性快照通道交付／按需查询／定时全量推送”，可多选；直接导出是APTS的下载操作，不依赖系统能力声明。数据集交付选直接导出或一次性快照通道交付；标签订阅选按需查询或定时全量推送，选项须同时符合开放类型和系统能力。

| 类型 | 对象 | 结果 | P0 方式 |
|---|---|---|---|
| 数据集交付 | 已确认人群快照 | 一次性固定名单 | 受控直接导出或已对接通道 |
| 标签订阅 | 一条动态人群的当前条件 | 患者清单（主患者标识、结果版本、计算完成时间） | 按需查询（间隔内复用最近成功并保留原计算时间）或定时全量推送 |

标签订阅仅包含主患者标识、结果版本、计算完成时间，不附加联系方式或处置建议。成功计算零人是合法空清单，结果版本和完成时间作为整体结果信息仍可查看；不能与计算失败混同。

数据集直接导出为 UTF-8 CSV（可带 BOM），每人一行，固定列：主患者标识、姓名、性别、年龄、就诊科室、联系电话、快照标识、快照名称、快照确认时间。身份和就诊摘要取快照固定内容；联系电话在首次形成交付时从已接入数据获取，有则导出用于患者触达，无则留空，不因此阻止导出。快照来源元信息每行重复。首行为列名，字段以英文逗号分隔、记录以CRLF换行；字段含逗号、换行或双引号时，整项用双引号包围，内部每个双引号写成两个双引号，稳定标识及电话按文本处理。不设自由字段选择，不导出完整证据和病历原文。通道交付的业务字段与该固定数据集相同，技术映射由底座处理。

第三方执行与联系授权全部在对方系统。若反馈要进 APTS，须先入底座再当普通患者数据用，不回写到某次开放配置。

API 鉴权、网络、字段映射、调用日志、失败重试不在 APTS Spec 实施范围。

---

## 11. 数据与配置

- 应用库保存数据集、指标、标签、批次、快照、开放配置等；查询直访 CDR／时序库。Demo 用虚拟数据模拟该分工，不建同步链路。
- 数据集由后台导入预置，前台只读。
- 指标被标签或衍生指标引用时不可删除；可停用。
- 全量／增量由系统按场景选择，不作为业务配置项。P0 自动识别按固定周期出完整批次，无「数据变化立即识别」。

已对接系统的“默认使用方”仅供开放表单预填，不是授权范围或可选系统的过滤条件。创建或编辑未启用配置时，选择系统仅在使用方为空时带出默认值，已有内容不覆盖；启用前可修改实际使用方。系统选择只校验可用状态和支持方式，不要求两处使用方相同。启用后开放配置的实际使用方仍锁定；修改系统默认使用方不回写任何已有开放配置。直接导出不选系统，使用方由用户填写。

### 11.1 最小字段与校验

| 对象 | 必填／自动／可选 | 校验与修改边界 |
|---|---|---|
| 通用业务对象 | 稳定标识自动生成且永久不复用；名称、责任组织必填；创建人／时间和最近经办人／时间自动记录；说明可选 | 名称去首尾空白后1至100字，说明至1000字；名称可重，列表显示标识；责任组织选门户组织，不等于患者范围 |
| 标签 | 基础／复合类型创建时必选且不可改；分类从预置目录选；规则；可选处置建议文本至2000字；识别开关默认关、开启时周期正整数天 | 草稿允许不完整，发布或已发布保存严格校验；处置建议仅标签详情及单标签患者详情展示，不产生任务，不随组合订阅下发 |
| 指标 | 字段绑定／衍生、来源与输入、值类型、单位、粒度、业务时间、患者／观察关联、有效性、去重和并列策略、可比性、适用判断方式 | 源字段元数据继承且只读；名称目录可编辑；适用判断方式只能选源能力的子集；衍生显式配置输入关联、取值、公式、结果类型单位；无可用判断方式不能保存；引用中的不兼容变更禁止 |
| 动态人群 | 名称、责任组织、纳入至少1个已发布可用标签、排除可空、患者范围；最近成功计算时间自动 | 同一标签不得重复或同时纳入排除；当前条件与成员计算时间分开展示；保存时采用当前条件完整计算结果，人工移除不写入成员或条件 |
| 快照 | 来源类型、来源批次／原快照、确认人／时间、计算完成时间、固定成员身份与就诊摘要自动；名称可编辑 | 成员不可原地修改，派生新快照；确认时间不冒充数据刷新时间；零成员不建快照 |
| 开放配置 | 类型、使用方、用途、绑定快照／动态人群、方式、责任组织、患者范围必填；有效期默认长期；启用与最近经办自动 | 用途去空白后1至500字；直接导出不强制系统，其他方式必须选系统；字段固定只读；启用后使用方、系统、对象、类型锁定；没有审批人或审批原因 |
| 已对接系统 | 名称、唯一稳定编码、默认使用方、业务／技术联系人、支持方式、底座通道引用、状态；最后线下校验时间只读显示或人工登记；用途备注可选 | 编码1至64位字母数字下划线短横线且创建后不可改；支持查询须填正整数分钟间隔，支持推送须至少一个正整数分钟周期；联系人是登记信息，不提供外发消息；不填地址密钥；停用传播全部关联配置 |

快照交付不随执行者当前患者范围扩大而增人。P0 固定全院；若后续范围缩小导致无权访问部分固定成员，则阻止整份下载并提示权限不足，不静默改写已固定的文件或快照。源标签证据权限被撤销时隐藏证据，但不删除快照身份与就诊摘要。

---

## 12. 非功能（P0）

- 院内页面展示足以识别患者和就诊背景的字段（姓名、患者标识、性别、年龄、就诊标识及科室）。不因此扩大对外开放字段。
- 门户撤权刷新后菜单／按钮不可见；标签授权与患者范围立即按后续请求生效。
- 报错：Toast + 后端提示语。不为失败单独做「展示旧名单」业务态。已保存对象不因一次失败被删。
- 不设响应秒数、全院耗时、并发等数值门槛。
- 核心流程基本可用性见 0.4。
- 后台审计、自动清理、备份 RPO／RTO 不在 P0。P2 清理策略尚未确定可清理对象，不在本轮补建清理功能或推定可删除业务记录；所有已形成历史快照和历史业务记录永久保留。

---

## 13. 来源场景覆盖（背景，不新增 Demo 功能）

四个来源场景逐项归类。 **产品已支持** = P0 页面／规则能配（输入须满足本文定义的值类型、单位、业务时间、关联与有效性约束）。 **依赖底座** = APTS 能引用，但指标／模型／身份须底座先给出。 **超出本期** = 不验收。  
「已支持」不是生产数据或医学有效性已验收。

### 13.1 门诊患者标签与运营

| 来源项 | 分类 | 对应 | 说明 |
|---|---|---|---|
| 干眼程度／阶段、乳腺分级、检查阳性 | 产品已支持 | TAG、指标比较／枚举 | 分级值由底座结构化 |
| 心血管多指标联合 | 产品已支持 | 且／或、同次就诊限定 | 「同时」须显式同次采集／就诊 |
| 义诊／活动圈选 | 产品已支持 | COH 纳入排除、OPEN 订阅 | 精准推送执行超出本期 |
| 建联意向字段 | 依赖底座 | 有字段则可比较 | 跨患者亲属关系不提供通用查询 |
| 首诊／复诊／诊前中后 | 依赖底座 | 已提供就诊类型则可判断 | 推导口径由底座或衍生指标声明 |
| 候诊剩余人数、已等待时长 | 依赖底座 | 有指标则可比较 | 固定周期识别不承诺实时 |
| 候诊预测、排队提醒、客服干预 | 超出本期 | — | 外部系统 |
| 短信／随访／宣教执行 | 超出本期 | — | 第三方患者服务 |

### 13.2 《灵枢经》课题版

| 来源项 | 分类 | 对应 |
|---|---|---|
| 年龄、性别、已结构化体质／结论 | 产品已支持 | 直接比较、最新值、历史存在 |
| 档案 BMI、腰臀等已有数值 | 产品已支持 | 比较；缺则指标库衍生 |
| 四诊采集、多模态模型、医学映射 | 依赖底座 | 规则只引用结构化结果 |
| 面象／脉诊设备工作站 | 超出本期 | 不进产品模块 |
| 中医知识库、方案库、临床有效性 | 超出本期 | 线下审定 |

### 13.3 区域居民推广版

| 来源项 | 分类 | 对应 |
|---|---|---|
| 档案、随访／复诊、设备衍生指标 | 产品已支持 | 规则 + 指标 |
| 居民身份与档案关联 | 依赖底座 | — |
| 无院区就诊建档对象纳入全院 | P1 | ADM 患者范围开关；P0 默认不纳入 |
| 全市居民端展示、采集任务 | 超出本期 | — |

### 13.4 心肺肾标签体系

| 来源项 | 分类 | 对应 |
|---|---|---|
| 阈值、参考范围比较 | 产品已支持 | 固定阈值或底座参考上下限 |
| 观察内组合、最近 N 次每次满足、首次异常、较前变化 | 产品已支持 | 记录条件组与纵向判断方式 |
| 图像几何量化、证候判断 | 依赖底座 | — |
| 持续／波动的具体医学公式 | 依赖底座或衍生配置 | 不由算子名称推断唯一算法 |
| 医学有效性 | 超出本期 | — |

这些场景使用本规格定义的通用比较、记录组、时间和衍生指标能力；未接入的医学输入必须标为底座依赖，不能伪装成前端已完成医学计算。

---

## 14. 验收矩阵

P0 操作级需求。Demo 列：完整 = 可操作；占位 = 有页但矩阵／配置简化；不实现 = 禁止做成可操作假功能。

| 需求 ID | 名称 | 页面 | 分期 | Demo | 样例数据 | 可验证标准 |
|---|---|---|---|---|---|---|
| WB-01 | 查看规模数字 | 工作台 | P0 | 完整 | 主故事或规则样例 | 展示待确认识别批次／人数、授权已启用、动态人群／快照、已发布标签；全 0 时仍展示数字 0 |
| WB-02 | 待办：识别结果已更新 | 工作台 | P0 | 完整 | 主故事或规则样例 | 进入识别中心对应标签最新批次；最新批次已确认或 0 人则不出现此待办 |
| WB-03 | 开放摘要跳转 | 工作台 | P0 | 完整 | 主故事或规则样例 | 点配置名称进入对应详情，点状态进入筛选列表；无配置不伪造待办 |
| WB-04 | 开放摘要 | 工作台 | P0 | 完整 | 主故事或规则样例 | 列出草稿／授权已启用／已暂停／已到期及依赖原因；旁注不表示对方已收到；无配置时空列表文案 |
| WB-05 | 模块入口 | 工作台 | P0 | 完整 | 主故事或规则样例 | 打开标签／识别／人群／数据开放；无权限的模块不出现 |
| WB-06 | 运营分析入口 | 工作台 | P2 | 占位 | 主故事或规则样例 | 进入占位页「模块开发中」 |
| WB-07 | 禁止首页处理 | 工作台 | P0 | 完整 | 主故事或规则样例 | 无复核、开放启停、导出按钮 |
| TAG-01 | 列表筛选 | 标签中心／详情 | P0 | 完整 | 主故事或规则样例 | 按类型、状态筛选；显示可用性；空列表引导新建 |
| TAG-02 | 新建基础标签 | 标签中心／详情 | P0 | 完整 | 主故事或规则样例 | 选类型基础，编指标条件，存草稿；无条件不能发布 |
| TAG-03 | 新建复合标签 | 标签中心／详情 | P0 | 完整 | 主故事或规则样例 | 可引用已发布基础标签（只读展开）+ 新条件；不能引用复合标签 |
| TAG-04 | 编辑基本信息 | 标签中心／详情 | P0 | 完整 | 主故事或规则样例 | 改名称、分类、责任组织与可选处置建议；类型创建后不可改；允许重名，显示稳定标识区分 |
| TAG-05 | 规则编辑 | 标签中心／详情 | P0 | 完整 | 主故事或规则样例 | 树摘要，点选右侧改参数；可分组、且／或、同次就诊、记录条件组；空组不能发布或保存为生效修改 |
| TAG-06 | 保存草稿／发布 | 标签中心／详情 | P0 | 完整 | 主故事或规则样例 | 已发布有编辑权限可保存生效修改，无待发布副本和版本历史；校验失败明确提示 |
| TAG-07 | 启停 | 标签中心／详情 | P0 | 完整 | 主故事或规则样例 | 人工停用后新计算停止；已有清单保留 |
| TAG-08 | 试算 | 标签中心／详情 | P0 | 完整 | 主故事或规则样例 | 命中人数 + 脱敏样例；不可用时不能当零命中，提示原因 |
| TAG-09 | 自动识别开关／周期 | 标签中心／详情 | P0 | 完整 | 主故事或规则样例 | 只对可用已发布标签；改周期后识别概览同步；本页不跑识别；未发布不能开 |
| TAG-10 | 依赖不可用 | 标签中心／详情 | P0 | 完整 | 主故事或规则样例 | 指标或基础标签停用后，本标签及引用它的复合标签提示原因；恢复依赖后自动恢复；人工停用保持 |
| TAG-11 | 删除标签 | 标签中心／详情 | P0 | 完整 | 主故事或规则样例 | 未引用草稿可删，已发布先停用；确认影响后删除；已发布且未人工停用的标签不能直接删，失效依赖不可继续算；历史永久保留 |
| REC-01 | 概览 | 识别中心／批次 | P0 | 完整 | 主故事或规则样例 | 列出已开启自动识别的标签及已停止的历史入口：周期、最近扫描、人数、确认状态；删除后只读历史入口标明原因，不计入当前自动识别标签数；无已开启自动识别的标签且无历史批次才显示空态 |
| REC-02 | 打开最新批次 | 识别中心／批次 | P0 | 完整 | 主故事或规则样例 | 左侧显示当前批次实际成员（初始6人），右侧画像；可移除／确认；0 命中不出现待确认 |
| REC-03 | 当前已不满足 | 识别中心／批次 | P0 | 完整 | 主故事或规则样例 | 李强证据区提示；触发时证据仍在；不自动移除、不阻止确认；无提示不表示已验证仍满足 |
| REC-04 | 移除 | 识别中心／批次 | P0 | 完整 | 主故事或规则样例 | 原因三档：当前已不满足／不应纳入本次运营人群／其他（可填说明）；不能添加；原始命中说明保留 |
| REC-05 | 确认成快照 | 识别中心／批次 | P0 | 完整 | 主故事或规则样例 | 剩余成员生成快照进入人群列表；本范围本批次立即只读；全移除后按 0 人规则：不生成空快照，批次仍只读 |
| REC-06 | 历史批次 | 识别中心／批次 | P0 | 完整 | 主故事或规则样例 | 只读，无移除／确认；新批次到来后上一批变历史 |
| REC-07 | 模拟下一周期 | 识别中心／批次 | P0 | 完整 | 主故事或规则样例 | Demo 提供「产生新批次」以便评审只读切换；失败 Toast |
| REC-08 | 站内提醒 | 识别中心／批次 | P0 | 完整 | 主故事或规则样例 | 非空最新结果一次「识别结果已更新」；空结果只记执行时间 |
| COH-01 | 列表 | 人群管理／详情 | P0 | 完整 | 主故事或规则样例 | 含识别来源快照与主动保存对象；类型、来源、人数；空态引导新建 |
| COH-02 | 新建圈选 | 人群管理／详情 | P0 | 完整 | 主故事或规则样例 | 纳入／排除已发布标签后检索；缺任一标签授权则不能检索，提示缺授权 |
| COH-03 | 检索中移除 | 人群管理／详情 | P0 | 完整 | 主故事或规则样例 | 只影响本次待保存名单；无「当前已不满足」 |
| COH-04 | 存动态人群 | 人群管理／详情 | P0 | 完整 | 主故事或规则样例 | 保存条件及未经人工移除的完整计算结果；0人可存；当下成员不带人工移除；无有效计算结果只存条件 |
| COH-05 | 存快照 | 人群管理／详情 | P0 | 完整 | 主故事或规则样例 | 人数须 > 0；0 人按钮不可用并说明 |
| COH-06 | 动态人群刷新 | 人群管理／详情 | P0 | 完整 | 主故事或规则样例 | 按当前条件重算；P0 手动；保留成功批次供查看；失败 Toast，不把失败显示成 0 人名单 |
| COH-07 | 从动态人群确认快照 | 人群管理／详情 | P0 | 完整 | 主故事或规则样例 | 生成独立快照；动态人群仍在；移除不写回条件；0 人不能确认 |
| COH-08 | 开放订阅 | 人群管理／详情 | P0 | 完整 | 主故事或规则样例 | 进入数据开放新建标签订阅并预填该动态人群；缺授权不能发起 |
| COH-09 | 详情 | 人群管理／详情 | P0 | 完整 | 主故事或规则样例 | 类型、来源、纳入／排除、成员、相关批次、刷新时间 |
| COH-10 | 从快照调整成员 | 人群管理／详情 | P0 | 完整 | 主故事或规则样例 | 以原件为起点移除，确认后生成新快照并记录来源；原件和旧交付不变，零人不生成 |
| COH-11 | 编辑动态条件 | 人群管理／详情 | P0 | 完整 | 主故事或规则样例 | 展示关联订阅及影响，有编辑权限可保存；不重审、不改写旧结果，下一次计算用新条件 |
| COH-12 | 历史快照导出入口 | 人群管理／详情 | P0 | 完整 | 主故事或规则样例 | 进入预填当前快照的直接导出配置；填写使用方用途、启用、下载；来源标签删除不阻止；仍校验导出按钮和患者范围 |
| OPEN-01 | 列表筛选 | 数据开放／详情 | P0 | 完整 | 主故事或规则样例 | 两类配置共用列表，查看状态与依赖原因；无配置引导新建 |
| OPEN-02 | 新建数据集交付 | 数据开放／详情 | P0 | 完整 | 主故事或规则样例 | 只能绑定已确认快照；动态人群不可选，无快照引导确认 |
| OPEN-03 | 直接导出方式 | 数据开放／详情 | P0 | 完整 | 主故事或规则样例 | 填使用方、用途，不强制选择系统；缺必填不能启用 |
| OPEN-04 | 通道交付 | 数据开放／详情 | P0 | 完整 | 主故事或规则样例 | 选支持该方式的已对接系统；系统停用不可启用 |
| OPEN-05 | 保存草稿 | 数据开放／详情 | P0 | 完整 | 主故事或规则样例 | 保存未启用配置；不完整可存，不能下载或计算 |
| OPEN-06 | 启用 | 数据开放／详情 | P0 | 完整 | 主故事或规则样例 | 校验通过后授权已启用，记录启用时间；缺权限、无效对象、截止已过则阻止 |
| OPEN-07 | 恢复 | 数据开放／详情 | P0 | 完整 | 主故事或规则样例 | 人工暂停或修改到期期限后手动恢复；系统／标签依赖未解除时不能恢复 |
| OPEN-08 | 直接导出 | 数据开放／详情 | P0 | 完整 | 主故事或规则样例 | 下载真实虚拟 CSV，记录固定内容、来源、经办人、使用方、用途、时间；草稿／暂停／到期不可下载，不用 Toast 冒充文件 |
| OPEN-09 | 新建标签订阅 | 数据开放／详情 | P0 | 完整 | 主故事或规则样例 | 绑定动态人群，选按需查询或定时全量，结果固定三字段；无动态人群引导创建，依赖不可用不能启用 |
| OPEN-10 | 暂停／到期 | 数据开放／详情 | P0 | 完整 | 主故事或规则样例 | 立即停止下载、查询、推送和未返回结果；已发出结果不撤回 |
| OPEN-11 | 修改开放配置 | 数据开放／详情 | P0 | 完整 | 主故事或规则样例 | 有编辑权限直接改用途、方式、周期、期限；下一次允许计算采用有效配置；启用后类型、使用方／系统及绑定对象锁定，无自由扩字段 |
| OPEN-12 | 系统停用 | 数据开放／详情 | P0 | 完整 | 主故事或规则样例 | 全部关联开放停止，显示原因；系统恢复后配置须人工恢复 |
| ANL-01 | 打开模块 | 运营分析 | P2 | 占位 | 主故事或规则样例 | 标题「运营分析」，正文「模块开发中」。P2 才做内部使用分析，见第 3 章 |
| ADM-01 | 数据集查看 | 系统管理 | P0 | 完整 | 主故事或规则样例 | 切换预置表，看来源、患者／就诊／记录／时间关联、字段与样例；无新增改删停；无「配置为指标」 |
| ADM-02 | 指标列表 | 系统管理 | P0 | 完整 | 主故事或规则样例 | 目录、绑定、状态、引用数；空态引导新建 |
| ADM-03 | 新建／编辑指标 | 系统管理 | P0 | 完整 | 主故事或规则样例 | 弹层；直接绑定或衍生；选适用判断方式；名称空、无判断方式不能存 |
| ADM-04 | 改绑定 | 系统管理 | P0 | 完整 | 主故事或规则样例 | 保存前列出受影响标签；新绑定用于后续计算；类型／单位／粒度不兼容则阻止 |
| ADM-05 | 停用／恢复指标 | 系统管理 | P0 | 完整 | 主故事或规则样例 | 依赖标签不可用；恢复后自动解除依赖层；人工停用的标签保持停用 |
| ADM-06 | 删除指标 | 系统管理 | P0 | 完整 | 主故事或规则样例 | 无引用才可删；有引用阻止并列出去向 |
| ADM-07 | 已对接系统 | 系统管理 | P0 | 完整 | 主故事或规则样例 | 登记名称、编码、默认使用方、开放方式、状态、查询间隔／推送周期声明；无地址、密钥、日志 |
| ADM-08 | 标签使用授权 | 系统管理 | P0 | 占位 | 主故事或规则样例 | 仅说明演示账号默认标签授权，不做可编辑矩阵 |
| ADM-09 | 患者范围 | 系统管理 | P0 | 只读 | 主故事或规则样例 | 展示当前「全院」及不含无院区建档对象；无多院区编辑 |
| PAT-01 | 从识别进入 | 患者画像 | P0 | 完整 | 主故事或规则样例 | 保留来源；可复核（仅最新待确认批次） |
| PAT-02 | 从人群进入 | 患者画像 | P0 | 完整 | 主故事或规则样例 | 只读，无复核按钮 |
| PAT-03 | 身份与就诊 | 患者画像 | P0 | 完整 | 主故事或规则样例 | 姓名、患者 ID、性别、年龄、就诊 ID、就诊科室 |
| PAT-04 | 来源条件与触发证据 | 患者画像 | P0 | 完整 | 主故事或规则样例 | 按进入来源显示标签逻辑或人群纳入／排除条件及相关证据；时间轴按业务时间；不展示全量病历 |
| PAT-05 | 当前已不满足 | 患者画像 | P0 | 完整 | 主故事或规则样例 | 仅明确不命中时出现；静默不表示已复核仍命中 |
| PAT-06 | 其他获授权标签 | 患者画像 | P0 | 完整 | 主故事或规则样例 | 放次级区域；未授权标签不出现 |
| PAT-07 | 复合证据 | 患者画像 | P0 | 完整 | 主故事或规则样例 | 可展开基础逻辑贡献；不能跳到未授权基础标签命中清单 |

### 14.1 完整蓝图中尚未做页的能力（禁止用 P0 页冒充已覆盖）

| 能力 | 分期 | 落点 |
|---|---|---|
| 专用模型治理（注册、版本、审批、结果接入） | P1 | **后续设计依赖**：系统管理未来页面；P0 只把底座模型输出当普通指标 |
| 可复用标签规则／实施模板 | P1 | 后续设计依赖 |
| 订阅定时增量推送、更新通知后拉取 | P1 | 后续设计依赖；合同仍在数据开放，传输形态由底座在需要时建设 |
| 多院区患者范围；全院纳入无院区建档对象 | P1 | 已有「患者范围」页，P0 固定全院且无开关 |
| 内部使用分析 | P2 | 已有运营分析入口，P0 占位 |
| 审计日志 | P2 | 有页面名，P0 不要求可操作 |
| 数据留存与自动清理 | P2 | 后续设计依赖，无 P0 清理页 |

### 14.2 Demo 禁止项

规则编辑其他布局切换；数据集增删改停；数据集页「配置为指标」；开放表单勾选标签；调用日志与推送失败；可配置多院区；可操作审计页；工作台趋势／人次／效果；把「授权已启用」做成对方已收到。

---

## 附录. 交付验收口径

本规格描述完整产品行为，配套领域词汇表只解释业务术语。Demo 的导航、布局、计算、状态、数据与验收均已在此写明，不依赖历史讨论或外部原型。文档校验通过不等于实际 Demo 验收通过；生成后必须逐条执行操作矩阵及下列关键场景，并分别记录通过、失败和未验证。


| 场景 | 前置与操作 | 必须得到的结果／关键反例 |
|---|---|---|
| 无审批开放 | 确认快照→新建交付→保存草稿→启用→下载 | 无审核人或审批步骤；草稿不下载，启用后获得与快照一致的CSV |
| 权限按钮 | 临时关闭演示编辑权限，尝试修改已发布标签；再恢复权限保存 | 无权限不能操作；恢复后校验成功即可生效，不需要再次发布；旧批次不变 |
| 主故事证据 | 11:00打开08:00批次李强 | 触发18、当前7，各自时间清晰；当前不满足只提示，仍可保留确认 |
| 全部移除 | 初始六人全部移除后确认 | 留下零保留确认记录，批次只读，无空快照，无工作台待办；不同于原本零命中 |
| 共享状态 | 移除李强确认五人，进入人群和开放创建交付 | 各页均为同一五人快照；导出五行，来源标识一致；重下载内容相同 |
| 快照派生 | 原快照移除一人后保存新快照 | 新旧两个对象、来源明确；旧交付成员不变，不能原地改原件 |
| 标签删除与历史消费 | 先形成快照及交付，再停用并删除来源标签；从旧快照新建直接导出 | 历史内容和标签标识保留、显示已删除；仍可下载／新建交付，不重算、不要求恢复标签 |
| 删除依赖 | 动态人群排除标签被删除，查询或推送 | 整条停止并显示具体原因，不能忽略排除扩大人群，不能返回缓存或假零人；历史结果只读 |
| 多重暂停 | 人工暂停订阅后停用、恢复其标签；另停用、恢复系统 | 标签恢复不能解除人工暂停；系统恢复也须在配置上人工恢复，全部阻断解除才可开放 |
| 条件变更 | 编辑被订阅引用的人群，去掉一项排除条件 | 显示关联影响，不重审；下一次允许计算用新条件，已发出结果不改写 |
| 有效期 | 启用有截止的开放，推进时钟至截止，延期后尝试查询 | 到期停止；编辑延期本身不恢复，点击恢复后才可计算／下载 |
| 联系方式 | 导出含有与缺失电话的患者 | 已有虚拟电话原样导出，缺失留空；不开放病历或自由字段选择 |
| 全规则能力 | 从规则详情载入每个能力样例，编辑参数、保存、重新打开、试算 | 参数摘要与值一致，逐行验证7.1及7.2正反例；首次、变化、时间、逐次复用与衍生不以文字占位 |
| 衍生指标配置 | 在指标库绑定数据字段或配置算术／日期差／窗口统计，再由标签引用 | 输入单位、粒度及关联受校验；缺输入、除0、不兼容绑定有明确结果，不生成伪零 |
| 失败与零结果 | 模拟失败，再执行一次成功零人查询 | 失败Toast、不生成新结果；成功零人有计算完成时间及结果版本；历史对象不被清空 |
| 可用性 | 键盘走完编辑、确认、启用、下载，200%缩放打开弹层 | 焦点可见且关闭后返回触发控件，主按钮可到达，错误可读、状态有文字 |

以上是待实施 Demo 的验收要求，文档本身不宣称浏览器交互已通过。

| 场景补充 | 前置与操作 | 验收结果 |
|---|---|---|
| 动态保存与人工移除 | 健康活动圈选三人，移除李强，选择保存动态人群；另从相同结果移除李强后保存快照 | 动态人群当下显示李强、林晓、吴平三人；快照只含林晓、吴平两人；提示说明人工移除不进入动态人群 |
| 连续日期与最近N次区分 | 加载连续日期样例，切换每日判断方式，再清空中间日观察 | 存在一次命中、全部有效观察不命中；缺日后均不命中；与跳过无法判断观察的最近N次分别验收 |

| 场景补充 | 前置与操作 | 验收结果 |
|---|---|---|
| 人群历史条件说明 | 健康活动圈选保存快照后，修改其纳入／排除条件或被引用标签阈值，再打开旧快照的来源说明 | 仍展示当时纳入成年、排除复诊筛选及当时展开条件；不强行指定唯一来源标签，不用当前条件重解释旧结果；订阅对外字段保持三项 |

| 场景补充 | 前置与操作 | 验收结果 |
|---|---|---|
| 默认使用方预填 | 新建订阅，使用方为空时选择患者服务系统；启用前改为健康活动组织组 | 先带出眼科随访组；允许改为健康活动组织组并启用，不以使用方不一致阻止选择系统 |
| 默认值不覆盖 | 先填健康活动组织组再选择患者服务系统；启用后修改系统默认使用方 | 已填内容不被覆盖；已启用配置实际使用方保持不变且不能直接编辑 |
